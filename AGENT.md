# 服务启动与局域网访问规则

当任务需要启动本项目的开发、预览或其他 HTTP 服务时，必须确保同一局域网内的其他设备可以访问该服务。

- 服务必须监听所有网络接口（`0.0.0.0` 或等效配置），不得仅绑定 `127.0.0.1` 或 `localhost`。本项目使用 Vite 时，应保留 `server.host: true`，或通过 `--host 0.0.0.0` 启动。
- 启动前应确认所用端口未被占用；不要擅自终止无关进程。若改用其他端口，需明确告知实际端口。
- 启动后必须同时报告本机访问地址和局域网访问地址，例如 `http://localhost:<端口>` 与 `http://<局域网IP>:<端口>`；不要把 `0.0.0.0` 当作供用户打开的访问地址。
- 至少验证服务正在监听非回环接口，并使用本机的局域网 IP 请求一次页面或健康检查端点。仅确认进程存在、端口监听或 `localhost` 可访问，不视为完成局域网访问验证。
- 如果条件允许，再从同一局域网的另一台设备实际访问。若无法进行跨设备验证，必须明确标注“本机局域网地址已验证，跨设备未验证”，不得宣称局域网访问已完全通过。
- 若访问失败，应依次检查监听地址、实际端口、本机局域网 IP、防火墙和访客网络/AP 隔离等因素。未经用户授权，不修改系统防火墙、路由器或其他全局网络设置。
- 服务启动后应保持运行，直到用户要求停止或任务明确结束；若使用临时后台进程，应告知其进程状态和停止方式。
